# VPN Server Setup

Полная автоматизированная установка высокопроизводительного VPN сервера с WireGuard, XRay и Shadowsocks.

## 🚀 Быстрый старт

### Docker (рекомендуется)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd vpn-project
   ```

2. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл
   ```

3. **Запустите с Docker Compose:**
   ```bash
   cd docker
   docker-compose up -d
   ```

## iOS (Shadowrocket)

See `IOS_SHADOWROCKET_GUIDE.md` for importing `vless://...` links and connecting from iPhone.

### Ручная установка

1. **Запустите скрипт установки:**
   ```bash
   sudo bash scripts/install/full-vpn.sh
   ```

## 📋 Архитектура

```
VPN Server
├── WireGuard (51820/UDP) - Максимальная скорость
├── XRay VLESS (443/TCP) - Защита от блокировок
└── Shadowsocks (8388/TCP/UDP) - Резервный канал
```

## 🔧 Конфигурация

### Переменные окружения (.env)

```bash
# Сервер
SERVER_IP=your-server-ip
SERVER_DOMAIN=your-domain.com

# WireGuard
WG_CLIENTS=5

# XRay
XRAY_UUID=generated-uuid
XRAY_PRIVATE_KEY=generated-key

# Shadowsocks
SHADOWSOCKS_PASSWORD=secure-password

# Мониторинг
GRAFANA_PASSWORD=admin-password
```

## 📊 Мониторинг

- **Prometheus**: http://localhost:9090
- **Grafana**: http://localhost:3000 (admin/admin-password)

## 🛠️ Управление

### Добавление WireGuard клиента

```bash
# Docker
docker exec vpn-wireguard wg-add-client client-name

# Ручная установка
sudo wg-add-client client-name
```

### Проверка статуса

```bash
# Docker
docker-compose logs -f

# Ручная установка
sudo vpn-status
```

## 🔒 Безопасность

- Автоматические обновления безопасности
- Fail2Ban защита от брутфорса
- UFW firewall
- Шифрование всех соединений

## 📈 Производительность

- **WireGuard**: 400-600 Мбит/с
- **XRay**: 250-400 Мбит/с
- **Shadowsocks**: 100-200 Мбит/с

## 🐳 Docker структура

```
docker/
├── Dockerfile.wireguard
├── Dockerfile.xray
└── docker-compose.yml

scripts/
├── install/
│   ├── wireguard.sh
│   ├── xray.sh
│   └── common.sh
└── maintenance/
    ├── backup.sh
    └── monitor.sh

config/
├── templates/
└── examples/
```

## 📝 Логи

- `/var/log/wireguard/`
- `/var/log/xray/`
- `/var/log/shadowsocks/`

## 🔄 Обслуживание

Автоматическое обслуживание запускается ежедневно:

- Очистка логов старше 7 дней
- Проверка дискового пространства
- Обновление GeoIP баз
- Перезапуск упавших сервисов

## 🤝 Вклад в проект

1. Fork репозиторий
2. Создайте feature branch
3. Commit изменения
4. Push в branch
5. Создайте Pull Request

## 📄 Лицензия

MIT License - см. LICENSE файл для деталей.

## ⚠️ Важно

- Всегда делайте бэкап перед обновлениями
- Регулярно обновляйте систему
- Мониторьте логи на предмет ошибок
- Используйте strong пароли

## 🆘 Поддержка

При проблемах проверьте:
1. Статус сервисов: `docker-compose ps`
2. Логи: `docker-compose logs service-name`
3. Конфигурации в `config/` директории
